import styled from 'styled-components';

export const CountryInput = styled.input`
	border: none;
	border-radius: ${({ theme }) => theme.borders.primaryRadius};
`;

export const CountryAddButton = styled.button`
	border: none;
	border-radius: ${({ theme }) => theme.borders.primaryRadius};
`;
